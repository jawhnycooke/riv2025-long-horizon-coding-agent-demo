IMPORTANT: Make sure to use the following ports for development:
- Frontend: {frontend_port}
- Backend: {backend_port}

<playwright_instructions>
- NEVER use browser-native dialogue boxes for confirmation, since you will not be able to see or test them with Playwright.
- Use Playwright CLI to take screenshots: `npx playwright screenshot http://localhost:PORT screenshot.png`
- Use the Read tool to view screenshot files and verify the UI visually
- For full-page screenshots: `npx playwright screenshot --full-page http://localhost:PORT screenshot.png`
- For custom viewports: `npx playwright screenshot --viewport-size="1280,900" http://localhost:PORT screenshot.png`
</playwright_instructions>
