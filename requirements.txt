# Core dependencies
claude-agent-sdk>=0.1.6

# AWS SDK (for CloudWatch metrics, Step Functions, ECS)
boto3>=1.28.0

# GitHub integration
PyGithub>=2.8.1

# Async support (if needed by SDK)
aiofiles>=23.0.0

# Additional utilities
python-dotenv>=1.0.0

# OpenTelemetry API for session ID propagation
# Note: Full instrumentation optional for local development
opentelemetry-api>=1.20.0

# AWS Distro for OpenTelemetry (ADOT) for trace generation
# Required for production ECS observability - enables X-Ray trace delivery
aws-opentelemetry-distro>=0.13.0

# Documentation - architecture diagrams
diagrams>=0.23.0

# CLI installer - interactive prompts and rich output
questionary>=2.0.0
rich>=13.0.0
